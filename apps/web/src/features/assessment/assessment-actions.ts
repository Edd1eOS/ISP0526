// Intentionally empty. The assessment now precedes the chat; results are
// stashed in sessionStorage and consumed by finalizeChatIntakeAction in
// ../intake-chat/chat-actions.ts. This file is kept as a placeholder so
// any cached imports fail loudly rather than silently resolving to a
// stale server action.
export {};

