// Institution / program seed data lives here as JSON.
// Every fact in the recommendation pipeline must trace to a source_id present
// in these files. LLMs translate / rephrase, they do NOT produce facts.
//
// File naming: <country>-<vertical>.json (e.g. au-universities.json).

export {};
