# 变更记录（Change Record）

> 每个非空 PR 必须在此追加条目。格式：`YYYY-MM-DD | <type>(<scope>) | <subject> | <PR #>`
> 此文件为 Release Notes 的事实源。版本 tag 前抓取相应区间内容。

---

## 2026-06-05 — Recommendation engine simulation harness

- `test(rules)`: add `packages/core/sim/` — a seeded random-profile simulation that drives `recommend()` over a configurable corpus (default 300 profiles) and captures every returned school for analysis. `random-profile.ts` provides a deterministic mulberry32 PRNG and a `randomProfile()` generator spanning the full input surface (GPA, IELTS/TOEFL/none, field, budget, country preference, tags, lifestyle, career, big_five). `recommend-sim.test.ts` aggregates per-school appearance counts split by band, band/selectivity/country distributions, coverage sparsity, and avg score, then writes `results/summary.json` + `results/report.md`.
- The harness doubles as a property test asserting the engine's hard invariants under fuzzing: no elite / highly-selective program ever lands in the safety band, every recommended school carries >= 3 cited reasons, per-band caps hold, and the empty-result rate stays below 50%. Initial run: 0 elite-safety violations across 300 profiles / 219 programs.
- Run via `pnpm --filter @isp0526/core sim` (tune with `SIM_RUNS` / `SIM_SEED`). Generated artifacts under `sim/results/` are gitignored and reproducible from the recorded seed.

---

## 2026-06-04 — Recommendation engine P1: admission_profile

- `feat(schemas/institution)`: add optional `AdmissionProfileSchema` to `ProgramSchema` with `selectivity` (open / standard / selective / highly_selective / elite), optional `competitive_gpa_4`, `required_tests` (gre/gmat/sat/act/lsat/mcat), `portfolio_required`, `research_required`, and `prerequisites`. The field is backward-compatible: every existing program continues to validate without modification, and the rule engine treats absence as "unknown".
- `feat(rules/bands)`: `estimateAdmissionDifficulty` now consults `admission_profile.selectivity` first when present, mapping each tier to an anchor difficulty (open 0.4 → elite 0.97) with tiny +0.01 nudges for `field_top` and `phd` so ties sort sensibly without crossing tier bands. Falls back to the existing reputation+gpa_min proxy when the field is absent.
- `feat(rules/dimensions/academic-fit)`: `score()` prefers `competitive_gpa_4` over `gpa_min` as the GPA reference when the program supplies the competitive figure, so an MIT-style 3.5 floor but 3.85 competitive cohort no longer reads as "comfortable" for a 3.6 applicant.
- `data`: seed `admission_profile` on the highest-risk rows in `programs.us.json` (all MIT and Stanford entries — elite), `programs.uk.json` (UCL CS/DS/Finance — highly_selective), `programs.sg.json` (NUS Computing and Data Science — highly_selective), `programs.hk.json` (HKU CS, HKUST Data Science & Technology — highly_selective), and `programs.ca.json` (UofT MSc CS — selective). 16 programs seeded. Unseeded programs continue to use the proxy.
- `test(rules/recommend)`: three new tests verifying (a) a declared-elite program is "stretch" even with low reputation_score, (b) `competitive_gpa_4` drives academic-fit (3.2 vs 3.9 GPA against a competitive_gpa_4 of 3.9 yields a clear delta), (c) seeded elite US programs and the seeded UCL/NUS programs cannot land in safety for any applicant. Suite now 19 files / 104 tests passing.

---

## 2026-06-04 — Recommendation engine P0 hardening

- `fix(rules/bands)`: add `safetyEligible(profile, candidate)` and apply it in `classifyApplicationBand` so selective programs (admission difficulty >= 0.78) cannot land in the "safety" band when the student is missing GPA evidence or, for language-required programs, has neither IELTS nor TOEFL on file. Elite programs (>= 0.88) continue to short-circuit to "stretch".
- `fix(rules/recommend)`: `fillEmptyBands` now takes the candidate index and refuses to relabel any candidate as "safety" if (a) its original band was "stretch" or (b) `safetyEligible` rejects it. This closes the redistribution path that previously could undo the per-candidate safety guard.
- `feat(rules/dimensions/academic-fit)`: add `toeflHeadroom` and combine with `ieltsHeadroom` via `Math.max`, so TOEFL-only programs and TOEFL-only students get a real language signal instead of degrading to NEUTRAL. `explain()` now emits a TOEFL line when both program and student have TOEFL data.
- `fix(rules/thresholds)`: introduce `gpaToleranceFor(candidate)` so elite programs use a 0.95 GPA tolerance and selective programs use 0.90, while normal programs keep the existing 0.85. Applicants far below an elite floor are now excluded by `gpa_far_below_min` rather than shown as reachable stretch.
- `feat(rules/recommend)`: extend `RecommendOutput` with a `coverage` field reporting passing count, post-country-cap count, post-fit-range count, per-band counts, a `sparse` flag, and English diagnostic reasons. Downstream UI can use this to surface "results are limited" notices instead of silently returning a thin set.
- `test(rules/recommend)`: five new regression tests covering: elite-program exclusion at the stricter GPA floor, coverage diagnostics shape, selective-TOEFL-only without student language can't be safety, selective program without GPA can't be safety, TOEFL headroom modulates academic-fit on TOEFL-only programs. Suite is now 19 files / 101 tests passing.

---

## 2026-05-27 — Data pipeline spec + ADR 0005

- `docs`: add `docs/data-pipeline.md` defining the ingest pipeline (fetch -> parse -> normalize -> validate -> diff -> draft -> human promote), the `packages/data-pipeline` workspace layout, the SourceModule contract, the per-step hard constraints, the CLI surface, the freshness policy, and a 9-step handoff checklist for the next agent. Add `docs/data-pipeline-sources.md` with the new-source registration template, common fetch constraints (UA, rate limit, robots, fixtures), PII red lines, GPA / tuition / field normalization rules, and the priority list of first sources to onboard (studyaustralia providers, UKCISA fees, Canada DLI list, per-university handbooks, gov.uk student visa). Add ADR 0005 proposing `packages/data-pipeline` as a separate pnpm workspace that imports core schemas but is never imported by runtime; promotion to production JSON is human-only. Update `docs/techstack.md` with the planned Data Pipeline section (undici, linkedom, robots-parser; no headless browser, no LLM SDK in this layer).

---

## 2026-05-26 — Voyage profile spec + remove turn cap

- `docs`: add `docs/voyage-profile-spec.md`, the authoritative coverage spec for the voyage step. It lists every dimension (goals / field / geography / funding / credentials / signals / personality_check), and for each subfield specifies the required positive cell, negative cell, and confirmation cell — together with per-dimension required thresholds, weighted completeness scoring, and the proper stop conditions. This is now the source of truth that the voyage prompt and adapter both reference.
- `refactor(intake-voyage)`: remove the hard 8-turn cap. The voyage no longer terminates by turn count. `maxTurns` is downgraded to a runaway guard (60) only. The system prompt's stop conditions are rewritten to: completeness >= 0.9 AND the six load-bearing dimensions each reach their positive+negative+confirmation thresholds; OR the user explicitly says they want to stop; OR two consecutive "I don't know" answers have been recorded into notes. Completeness scoring is rewritten as a weighted per-dimension threshold (0/1) rather than an axis-by-axis fraction.
- `refactor(intake-voyage)`: `voyage-stage.tsx` progress bar is now driven entirely by LLM-reported completeness (no turn ratio). `canEarlyFinalize` no longer references a turn ceiling — it just unlocks the manual "结束航行" button once the user has answered three turns or the model thinks coverage > 0.6.

## 2026-05-26 — Intake voyage (first-person sailing UI)

- `feat(intake-voyage)`: dedicated voyage prompt + adapter that fill an "extremely detailed" `VoyageProfile` (positive AND negative preferences, dealbreakers, decisive factors, upload confirmation). New `packages/core/src/ai/prompts/voyage-question.ts` defines `VoyageProfileSchema` covering goals (motivations / post-grad intent / PhD intent / employability vs passion / urgency), field (primary / secondary / avoid / capstone-vs-thesis / pace / teaching likes+dislikes / assessment likes+dislikes / class size / supervisor / peer competitiveness), geography (target / excluded countries with love-to-hate direction and reasons, city size, climate likes+dislikes, distance, safety, food, transit, accommodation, social scene), funding (budget + flexibility + sources + scholarship priority + work intent + price-vs-rank), credentials (gpa with scale and self-confidence, language with sub-scores and retake plan, other tests, work years, research experience), signals (must-haves, avoid list, liked / disliked institutions, decisive factors), and a 5-axis personality_check. `VoyageQuestionSchema` covers `choice | multi | scale | number | free` with optional landmark hint. `nextVoyageTurn` adapter wires it to `getModel("extraction")`; `apps/web/src/lib/ai/google-voyage.ts` provides the SDK binding via `generateText` + `Output.object`.
- `feat(intake-voyage)`: server action `nextVoyageTurnAction` + projection helper `projectVoyageToClarifyPatch` that maps the rich profile down to existing `ClarifyPatch` overlays without overwriting captured values (gpa / ielts / budget / city_size / teaching_style / preferred_tags). Voyage stage now uses this dedicated path instead of the legacy chat action so the LLM can fill the full profile directly.
- `feat(intake-voyage)`: `voyage-stage.tsx` rewritten to dispatch on `question.kind` — `choice` (chip row + free fallback), `multi` (toggle chips with confirm), new `ScaleInput` (5-button Likert with model-supplied poles), `number`, `free`. Reads upload summaries from `isp_intake_upload_summaries_v1` (added by `upload-step.tsx`) and feeds them into the prompt so the LLM can ask confirmation questions about extracted values before importing them. Persists `VoyageProfile` and history per session.
- `feat(intake-voyage)`: water-surface polish — sun reflection lane (vertical pulsing light strip from horizon centre), three horizontal wave bands at different depths and speeds, V-shaped boat wake with foam-pulse keyframe under the viewer, plus a new `reef` waypoint silhouette for negative-preference questions. All animations respect `prefers-reduced-motion`.

---

## 2026-05-26 — Intake hub: rocket launcher + chat resume gate

- `feat(intake)`: Hub 三门入口换成单一长按蓄力火箭（`features/intake-hub/rocket-launcher.tsx`）—指针/键盘 hold 1.5s 蓄满后火箭起飞 + 渐隐过渡进入 `/intake/chat`；释放重置。角落保留小号"想直接填表？"链接到 `/intake/form`
- `feat(intake-chat)`: 聊天模块开场新增简历 gate（`features/intake-chat/resume-upload-panel.tsx`）：先问"有简历吗？"，上传 PDF/.docx 后用 Gemini 抽取并映射到 `ClarifyPatch`（`target_level/target_field/gpa/ielts_overall/annual_budget_aud`），写入 `sessionStorage` 后再启动 LLM 第一轮，跳过已锁定字段；不上传则正常开聊
- `feat(intake-chat)`: 聊天页两栏布局，右侧持久上传面板，任意时刻拖入文件都会合并进 accumulated 并触发"已补充 N 条"提示
- `feat(intake-chat)`: 新增 server action `extractResumeForChatAction(text)` —— 走 `extractProfileFromText` + `enrichExtraction`，输出仅是 `ClarifyPatch`，不写入 intake session store（与 upload door 解耦）
- `chore(i18n)`: `intakeHub` 文案改写为火箭蓄力引导（zh/en），删除旧的 `doors.form/upload/chat` 子树

## 2026-05-26 — Landing globe (SVG scrolling strip, supersedes R3F attempt)

- `feat(landing)`: Landing 页面 hero 改为纯 SVG 滚动地球——把等距矩形大陆条纹（含北美 / 中美 / 南美 / 欧亚 / 非洲 / 澳洲 + 不列颠 / 日本 / 马达加斯加 / 印尼 / 菲律宾 / 冰岛 / 斯里兰卡 / 古巴 / 塔斯曼尼亚 / 新西兰 + 南极 + 撒哈拉 / 亚马逊 / 刚果 / 阿拉伯沙漠 / 澳洲内陆 biomes + 落基 / 安第斯 / 阿尔卑斯 / 喜马拉雅山点）并排平铺两份，在圆形 clipPath 内做 36s 线性 `translateX(0 → -640)` 无缝循环，配合径向高光假装球面
- `feat(landing)`: 地标黏土 SVG（埃菲尔铁塔 / 自由女神 / Silicon Valley 苹果环 + 棕榈 + 写字楼 / 富士山 + 塔 / 三只跳跃袋鼠 / 飞船）分 4 阶段在 24s 周期里轮流弹出，时序大致对齐当前可见区域
- `feat(landing)`: 尊重 `prefers-reduced-motion`：停止条带滚动 / 袋鼠 / 飞船 / 弹出动画
- `chore(deps)`: 移除 three、@react-three/fiber、@react-three/drei、@types/three（-47 包）；本次 hero 无新增运行时依赖
- `docs(adr)`: 新增 ADR 0004（决定回到 SVG 方案），ADR 0003 标记为 Superseded

## 2026-05-25 — Plan page (selection + pyramid + timeline + checklist)

- `feat(plan)`: 新增 `/r/[code]/select` 选校页（2 冲 / 3 稳 / 1 保），跳转 `/r/[code]/plan?picks=...`
- `feat(plan)`: 方案页含金字塔布局 + 向下箭头优先级 + 时间轴滑块 + 资料清单
- `feat(core)`: ProgramSchema 增加可选 `deadlines`；新增 country-calendar 兜底
- `feat(ai)`: PlanChecklistSchema + adapter，LLM 生成的清单走 Zod 校验
- `chore(report)`: 报告页移除追问区与 ContactCard，新增"制定方案" CTA

## v0.0.0 — Baseline（仓库初始化）

| 日期 | 类型 | 摘要 | PR |
|---|---|---|---|
| 2026-05-23 | docs(spec) | initial product spec v1.0 | — (pre-repo) |
| 2026-05-23 | chore(repo) | initialize repo skeleton with docs, legal, copilot instructions | — (initial commit) |
| 2026-05-23 | chore(mcp) | add Figma MCP server (Framelink) for design token extraction | — (direct main) |
| 2026-05-23 | docs(spec) | section 7 rewritten as Warm Claymorphism design system with full token table (colors / radii / shadows / typography) extracted from Meng To reference + warm palette adaptation | — (direct main) |
| 2026-05-23 | feat(scaffold) | sprint 0: scaffold `apps/web` (Next.js 16 + TS strict + Tailwind v4) and `packages/core` (rules/ai/data/schemas); write warm claymorphism tokens into `globals.css`; add token-preview landing page; add i18n message files (zh/en); add Supabase env helper + `.env.example`; add GitHub Actions CI (lint+typecheck+test+build) | feat/scaffold-web-and-core |
| 2026-05-23 | refactor(spec) | business model adjusted: users no longer routed to commercial agents directly. Reports now end in a low-key "want to talk to someone?" card that opens a Contact page (WhatsApp / WeChat to our team + report ID). Agents remain a backend referral channel, invisible to end users. Renamed `TransferCode` -> `ReportCode`, updated FR-5.3 / FR-6 / FR-9.3 / NFR-5.5 / data model / KPIs / risks accordingly. i18n message files updated. | feat/scaffold-web-and-core |
| 2026-05-23 | docs | sync spec 7.2.1 color/shadow tables with desaturated palette (round 2); update techstack.md to Next 16 / TS 5.9 / Node 22 / pnpm 11.2.2 and document `allowBuilds` constraint | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | sprint 1 step 1: introduce Zod schemas (`ids`, `source`, `institution`, `student-profile`, `scoring`) as the single source of truth for the rule engine and the LLM contract; add 5 Go8 placeholder rows in `packages/core/data/{universities,programs}.au.json` with `source_id` citations; wire static data loader that validates JSON at module init; implement `thresholds.applyHardThresholds`, `bands.classifyBand`, and the first scoring dimension `academic-fit` with 7 unit tests; wire vitest, allow esbuild postinstall in `pnpm-workspace.yaml` | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | sprint 1 step 2: complete 7-dimension scoring engine — add `personality`, `lifestyle`, `career`, `budget`, `tag_boost`, `reputation` pure scoring functions with shared `__fixtures__` and 26 colocated tests; add weight personalization (`modulate.ts`, doubles budget weight on high salary sensitivity); add aggregator `score.scoreCandidate` that combines the 7 dimensions into a validated `Score` (per-dimension breakdown, 0..100 final score, band, cited reasons); add `recommend.recommend` pipeline (hard thresholds -> score -> sort -> bucket caps 5/10/5); 41 tests green | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | sprint 1 step 3: AI adapter scaffold — add `ai/result.ts` Result type with `AIError`; `ai/config.ts` env-driven `getModel(role)`; `ai/prompts/recommendation-narrative.ts` (Zod schema + system prompt + structured user-prompt builder, bilingual zh/en); `ai/post-filter.ts` rejecting narratives whose citations are not in the known source-id set; `ai/adapters/narrative.ts` injectable `GenerateObjectFn` so the Vercel AI SDK can be wired without coupling business code. 53 tests green; no new runtime dependencies | feat/scaffold-web-and-core |
| 2026-05-23 | chore(core) | drop trailing `.js` extension from all relative import specifiers under `packages/core/src/**` so the package can be consumed as raw TypeScript via Next.js `transpilePackages` without precompiling. Pure mechanical rewrite (tsc bundler resolution accepts both forms); 58 tests still green | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | sprint 1 step 4: end-to-end free-tier flow. Add `packages/core/ai/templates/narrative-template.ts` producing the same `RecommendationNarrative` schema as the LLM adapter (deterministic, zero API cost), `packages/core/reports/code.ts` generating 6-char Crockford-style `ReportCode`. Add `apps/web` intake form (server action + `useFormStatus`) → `recommend()` pipeline → in-memory report store → `/r/[code]` report page with `ScoreCard` + `ContactCard` + per-dimension breakdown. Landing CTA now routes to `/intake`. `next.config.ts` gets `transpilePackages: ["@isp0526/core"]`; `zod` added to web dependencies | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | expand AU program seed data 5 → 21 programs across 5 universities (ANU/UniMelb/UNSW/USyd/UQ) covering IT, Computing, Data Science, Business, Finance, MBA, Civil/Electrical/Mechanical Engineering, Design, TESOL. All entries still carry `placeholder sticker price; verify before launch`; URLs point to canonical program pages | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | file-backed report store under `apps/web/.data/reports/{code}.json` (gitignored) with per-process in-memory cache; replaces the volatile globalThis Map so restarting `next dev` no longer 404s historical report codes. `saveReport` / `loadReport` are now async; callers updated. | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | intake form `target_field` becomes a curated dropdown (`apps/web/src/features/intake/field-options.ts`) whose 11 values are 1:1 with `programs.au.json` `field` strings; removes the free-text trap that was zeroing out the field-match dimension | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | add batch narrative pipeline: `prompts/recommendation-narrative-batch.ts` (Zod schema wrapping `narratives: Array<RecommendationNarrative>`) + `adapters/narrative-batch.ts` reusing per-item `filterNarrative` and returning a `{ narratives, rejected }` map so callers can fall back per slot. 63 tests green (added 5) | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | wire Gemini 2.0 Flash (free tier) via Vercel AI SDK (`ai` ^6, `@ai-sdk/google` ^3). `apps/web/src/lib/ai/google-narrative.ts` injects `generateObject` into the core batch adapter. `intake-actions.ts` calls LLM first and falls back to the deterministic template for any rejected / missing item, or for the whole batch when the API key is absent or the call throws. `.env.example` updated; `docs/techstack.md` AI section rewritten | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | sprint 2 step E1a: restructure `/intake` into a 4-door hub (form / upload / voice / chat) with claymorphism cards; move the structured form to `/intake/form` so the entry-point can host multiple intake channels. Voice / chat tiles render as "coming soon" until E2/E3 land | feat/intake-multimodal |
| 2026-05-23 | feat(web) | sprint 2 step E1b: `/intake/upload` page + `UploadDropzone` client component (drag/drop, multi-file, per-file progress + status badge). PDF text extraction runs client-side via `pdfjs-dist` in main-thread mode (`disableWorker:true`) to sidestep Turbopack worker plumbing. Extracted text currently logs to dev console; AI hook-up follows in E1d/E1e | feat/intake-multimodal |
| 2026-05-23 | feat(core) | sprint 2 step E1c: intake extraction layer in the AI package - `prompts/intake-extraction.ts` defines `ExtractedProfileSchema` (every field wrapped with `{value, confidence, source_excerpt}`), bilingual system prompt forbidding fabrication; `adapters/intake-extraction.ts` parses Zod, drops sub-floor confidence (default 0.4), surfaces `Result<ExtractedProfile, AIError>`. 7 new unit tests cover empty input, parse path, confidence floor + override, schema invalid, generation error, default exported. 70/70 tests green | feat/intake-multimodal |
| 2026-05-23 | feat(web) | sprint 2 steps E1d + E1e: end-to-end upload -> review -> submit flow. New file-backed intake session store under `apps/web/.data/intake-sessions/{uuid}.json` (gitignored, in-process cache). New server action `startIntakeFromTextAction` calls Gemini via a new `buildGoogleExtractionGenerator` (binds `ExtractedProfileSchema` to Vercel AI SDK `generateObject`), persists the session, returns the session id. Upload dropzone now POSTs combined extracted text to that action and routes to `/intake/review/[id]`. New `/intake/review/[id]/page.tsx` server component loads the session and renders the new client `ReviewForm`: split layout (left = source label + scrollable original text + AI notes; right = the canonical intake fields, each pre-filled from the extracted profile and tagged with a confidence badge - green "AI 高置信" for >=0.7, yellow "请核对" for <0.7, gray "已修改" once the user edits, with the source excerpt rendered as a quoted caption below the input). Free-text discipline matched onto the curated FIELD_OPTIONS via substring; unmatched values fall back to manual selection. The review form submits to the existing `submitIntakeAction` so the rest of the report pipeline is unchanged | feat/intake-multimodal |
| 2026-05-24 | feat(web) | sprint 2 step E1f (partial): DOCX upload support via mammoth ^1 browser bundle. New `apps/web/src/lib/docx/extract-docx-text.ts` lazy-imports `mammoth/mammoth.browser.js`, runs `extractRawText({arrayBuffer})`, returns plain text. Dropzone now accepts `.pdf` and `.docx` (mime + extension fallback for browsers that drop the docx mime), routes each file to the right extractor, and the rest of the pipeline (combine -> server action -> review) is unchanged. Added a local `.d.ts` shim because mammoth ships no types for the browser entry. Image-based intake (Gemini Vision) intentionally deferred | feat/intake-multimodal |
| 2026-05-24 | feat(web) | sprint 2 steps E2a + E2b + E2c: voice intake channel. New `/intake/voice` page; client `VoiceOrb` (claymorphism orb, idle / requesting / listening / captured / uploading / error states, 60 s auto-stop with on-orb countdown, replay + redo + submit controls). Recording uses MediaRecorder with a candidate-mime list (webm/opus -> webm -> mp4 -> mpeg) so it works in Chromium, Firefox, and Safari. New server action `startIntakeFromAudioAction(formData)` reads the audio Blob, calls Gemini 2.0 Flash via `generateText` with a multimodal `file` part (transcribe-only prompt, original-language preserved), then reuses the existing `extractProfileFromText` + `buildGoogleExtractionGenerator` to fill `ExtractedProfile`, persists an intake session with `source.kind='voice'`, and routes the user to the existing `/intake/review/[id]`. Hub door #3 unlocked. 15 MB hard cap server-side for safety | feat/intake-multimodal |

---

<!-- 未来条目追加到上方 v0.0.x 区段，按时间倒序 -->
| 2026-05-24 | fix(intake-extraction) | sprint 2 voice-channel calibration: relax the extraction system prompt so currency-less budget phrases (e.g. "20��") default to AUD at confidence 0.5..0.65, and broad Chinese discipline terms (e.g. "����", "�̿�") normalize to the closest English discipline name at confidence 0.55..0.7; also widen the confidence ladder to 0.4..0.6 for "supported but requires assumption". Add dev-only `console.info` of raw LLM output before the confidence floor for debug visibility. 70/70 core tests still green | feat/intake-multimodal |
| 2026-05-24 | feat(intake-clarify) | sprint 2 step C1: add post-intake clarification chat as the mandatory last leg of every intake channel. New feature module apps/web/src/features/intake-clarify/ with: clarify-schema.ts (Zod ClarifyPatchSchema + ClarifyTurnSchema constrained to FIELD_OPTIONS / teaching style / city size / preferred tags enum values), clarify-prompt.ts (Chinese-only warm advisor system prompt, one-question-per-turn, normalizes vague answers to enums, defaults currency-less budget to AUD), clarify-actions.ts (server action calling generateObject + gemini-2.0-flash with the turn schema, returns reply + optional patch + done flag), clarify-chat.tsx (controlled chat surface, scroll-to-latest, Enter-to-send). Refactor review-form.tsx to controlled state so chat patches can update inputs live, replace edited Set with 3-state origin map (ai / confirmed / edited), add green "AI ׷����ȷ��" badge for chat-patched fields. Review page now passes sessionId. Build green. | feat/intake-multimodal |
| 2026-05-24 | fix(intake-clarify) | sprint 2 step C2 UX hardening: (1) add deterministic fallback engine (apps/web/src/features/intake-clarify/clarify-fallback.ts) that picks the next priority question and parses replies via keyword/regex when the LLM throws (quota / network / no key) so the chat never dead-ends. Returns assistant text with a hidden [[ask:KEY]] marker so the next turn knows which field was just asked. (2) clarify-actions catches any LLM exception, logs to server console only, returns a friendly "AI ��ʱ����" prefix and switches to the fallback; never surfaces SDK stack traces to the UI. (3) clarify-chat auto-opens by calling the server with empty messages so the very first turn is a concrete question rather than a generic "I have a few questions". Softens user-bubble color (var(--color-bg) + 1px primary border) so it no longer reads as a quick-reply button. Surfaces a degraded banner when running off the fallback. (4) review page copy now distinguishes 0-extracted from N-extracted ("AI û��ԭ����ֱ�������κ��ֶΡ�������ļ�����㲹�ϡ�"). Build green. | feat/intake-multimodal |
| 2026-05-24 | fix(intake-clarify) | sprint 2 step C3 follow-up: (1) clarify-fallback now tracks per-field attempt count in the marker ([[ask:KEY:N]]) and gives up after 2 failed parses, advancing with a "this one skipped, edit in form later" ack. Skip-intent words (��֪��/���/����/����һֱ��/û��� etc) immediately advance. (2) parseBudget broadened to USD/HKD/EUR/GBP/JPY/SGD/CAD/AUD with explicit rates; bare "20��" defaults to RMB; rejects joke currencies (��Ͳ�Τ/ί������); accepts �� magnitude; clamps invalid AUD range so unparseable values trigger the skip-after-2 path instead of looping. (3) New regex enricher (apps/web/src/lib/ai/intake-enricher.ts) runs after every voice/upload extraction and back-fills target_field, target_level, gpa, ielts_overall, annual_budget_aud from common Chinese/English keyword patterns; conf 0.5 so the badge stays "AI ��ȷ��". Wired into upload-actions and voice-actions. This finally makes the voice path useful even when Gemini extraction returns zero. (4) ClarifyChat moved above the source+form grid so it is the first interactive element on the review page; was previously below the fold. Build green. | feat/intake-multimodal |
| 2026-05-24 | fix(intake-clarify) | sprint 2 step C4 hotfix: (1) marker [[ask:KEY:N]] was leaking into chat bubbles because MARKER_RE in clarify-chat.tsx only matched the old [[ask:KEY]] format; widened to /\[\[ask:\w+(?::\d+)?\]\]/g. (2) Dropped the per-message "AI ��ʱ���ˣ������ѻ��ʴ����" prefix; the chat-header banner already signals offline mode, repeating it on every reply was noisy. (3) parseNumber and the enricher CURRENCY table now recognize the k-suffix (60k -> 60000, 60k����� -> 12766 AUD, 80k AUD -> 80000). Build green. | feat/intake-multimodal |
| 2026-05-24 | fix(intake-clarify) | sprint 2 step C5: (1) Currency default flipped to RMB on a Chinese-first platform. parseBudget no longer treats bare numbers as AUD; bare 60000 -> 12766 AUD via 1/4.7. Enricher CURRENCY table same: bare "60k" + bare 4-6 digit numbers default RMB. AUD only applies when keyword AUD/��Ԫ/�ı� appears. (2) Skip ack now echoes user context. When the student says e.g. "��û�� ���У԰����" we strip the skip phrases, see "��� У԰����" leftover, and reply "�ã����������ᵽ�ġ���� У԰������������û���ѡ�����Ϊ�ο�����������������" instead of the curt "�ã�����������" �� addresses the "��Ϊʲô����¼�ҵĴ�" complaint. (3) Retry budget raised from 2 to 3 attempts so users get a second clarifier before the field is force-skipped. Build green. | feat/intake-multimodal |
| 2026-05-24 | fix(intake) | sprint 2 step C6: switched all Gemini text callers (extraction, narrative, clarify-chat) from gemini-2.0-flash to gemini-2.5-flash. Diagnosed via direct REST probe: the user project has limit=0 on every gemini-2.0-flash free-tier quota (input tokens / RPM / RPD) but 2.5-flash returns 200 OK with the same key, so 2.5-flash is the only viable text model for this project. Model id is now overridable via GOOGLE_TEXT_MODEL_ID env var. Voice transcription was already on 2.5-flash (via GOOGLE_VOICE_MODEL_ID). Build green. | feat/intake-multimodal |
| 2026-05-24 | chore(intake) | sprint 2 step C7: dropped voice transcription feature. Voice intake was UX-equivalent to filling the form + chat clarifier but added an unreliable Gemini transcription step (single point of failure, recorder/codec edge cases, no real differentiator). Removed the door card from /intake landing; narrowed IntakeSource type to "upload" | "chat"; relocated apps/web/src/app/intake/voice/ and apps/web/src/features/intake-voice/ to apps/web/.attic/ (gitignored) so the user can delete via Explorer when ready. Route /intake/voice no longer compiled by Next. Build green. | feat/intake-multimodal |
| 2026-05-24 | feat(intake-form) | sprint 2 step C8: rewrote /intake/form as a one-question-per-card wizard with 8 steps and progress dots. Five distinct interaction types (was: 100% native dropdowns): (1) large tile grid (target_level, city_size), (2) single-select chip cloud (target_field), (3) styled range slider (GPA, IELTS), (4) RMB-denominated budget slider with live AUD conversion (��X ��/�� -> A$Y on the fly), (5) segmented pill control (teaching_style), (6) multi-select chip cloud with check marks (preferred_tags). Cards slide in horizontally on next/back via CSS keyframes with prefers-reduced-motion fallback. Progress dots double as click-to-jump nav. Submit constructs FormData client-side and calls the existing server action so intake-schema and rule engine stay untouched. Old intake-form moved to apps/web/.attic/intake-form-old.tsx. Build green. | feat/intake-multimodal |
| 2026-05-24 | feat(intake-chat) | sprint 2 step C9: launched the third intake door at /intake/chat (was: ready=false). Three-question card-stack wizard (background / goal / constraints) with progress dots and a styled textarea per step; on submit concatenates Q/A pairs and routes through the same startIntakeFromTextAction pipeline as the upload door (source=chat). New feature module apps/web/src/features/intake-chat/. Flipped the chat door tile on /intake to ready=true. | feat/intake-multimodal |
| 2026-05-24 | feat(report-pdf) | sprint 2 step C10: server-rendered PDF export at GET /r/[code]/pdf via @react-pdf/renderer (Node runtime). Document covers report code, generation date, student profile snapshot, and Match/Stretch/Safety sections with headline + summary + pros/cons per program. Noto Sans SC registered from jsDelivr CDN so Chinese renders correctly; hyphenation disabled to keep CJK lines intact. Download PDF button added to report header. Bundle: +@react-pdf/renderer@^4.3.0. | feat/intake-multimodal |
| 2026-05-24 | feat(analytics) | sprint 2 step C11: PostHog instrumentation behind an opt-in privacy banner. Client provider only loads posthog-js after the user clicks accept; consent is persisted in localStorage so the banner never reappears. Pageview + pageleave only, autocapture and session recording disabled to keep payload small. Banner styled with claymorphism tokens. Approved/denied build scripts list in pnpm-workspace.yaml (core-js, protobufjs as not-needed). Bundle: +posthog-js@^1.376.0. | feat/intake-multimodal |
| 2026-05-24 | feat(report-poster) | sprint 2 step C12: WeChat-friendly share poster at GET /r/[code]/poster.png. 750x1334 PNG rendered server-side via satori (JSX to SVG) then resvg (SVG to PNG); claymorphism palette, three top tiles with band labels (����/��һ��/����), report code, and a soft prompt to ask the sender for the link. Noto Sans SC fetched once and cached in module scope. Share button added next to the PDF download on the report header. Bundle: +satori@^0.12.0, +@resvg/resvg-js@^2.6.2. | feat/intake-multimodal |

| 2026-05-24 | fix(intake,pdf,poster) | hotfix batch: (1) intake-schema FormData parser now treats absent fields as undefined and the optionalNumber union accepts undefined, so the wizard's optional steps no longer throw ZodError; (2) next.config.ts marks @resvg/resvg-js and @react-pdf/renderer as serverExternalPackages so Turbopack stops trying to statically resolve their platform-specific native bindings; (3) new pps/web/src/lib/fonts/noto-sans-sc.ts loads the @fontsource WOFF via process.cwd()-anchored candidate paths (Turbopack rewrites import.meta.url to a virtual path), materializes a stable copy under os.tmpdir() for react-pdf which requires a file path src. Bundle: +@fontsource/noto-sans-sc@^5.2.9. | feat/intake-multimodal |

| 2026-05-24 | feat(i18n) | sprint 2 step C13 (P1-8): next-intl integration. Routes are now /zh/* and /en/* with always-on locale prefix and zh as default; root / redirects to /zh via the proxy (Next 16 renamed the middleware convention). New src/i18n/{routing,request,navigation}.ts modules. App tree restructured under app/[locale]/ so layout, landing, and intake pages run through NextIntlClientProvider; the locale-free /r/[code]/pdf and /r/[code]/poster.png route handlers stay at the root so QR/share links keep working without a locale segment. Landing page and intake hub now read all copy from messages/{zh,en}.json. Deeper migration of the form wizard, chat, upload, and report internals stays as follow-up work. Bundle: +next-intl@^4.12.0. | feat/intake-multimodal |

| 2026-05-24 | feat(rules,data) | sprint 2 phase 1 (M1-M4): global multi-country expansion. M1: recommend() now hybrid-classifies bands — keeps classifyBand() per-candidate but redistributes by academic_fit rank when any band would otherwise be empty (>=3 survivors), so "冲刺/主推/保底" buckets stay populated. M2: data loader becomes shard-aware (universities.<cc>.json + programs.<cc>.json merged at module init); de-AU UI copy removed from chat-intake placeholder and PDF cover title (now "你的留学院校推荐"). M3: new visa_feasibility dimension (0..1) blending typical visa processing weeks, post-study work years, and migration_friendly tag bonus; modulated higher when migration_intent>=4. Weights rebalanced to 8 dims summing to 1.0 (academic 0.25, personality/lifestyle/career/visa 0.13, budget 0.10, tag_boost 0.08, reputation 0.05). New packages/core/data/visa-routes.json cites Home Affairs (AU 500), gov.uk Student Route, IRCC Study Permit. M4: seed data adds 3 UK universities + 18 programs (Imperial, Edinburgh, Manchester) and 3 CA universities + 18 programs (UofT, McGill, UBC); all carry "verify before launch" stickers and AUD sticker prices converted from GBP/CAD. 75 tests green. | feat/intake-multimodal |

| 2026-05-25 | feat(ui,ai) | sprint 2 phase 2 (M5-M8): result page enrichment + IM-style follow-up chat. M5: per-recommendation breakdown panel rewritten as an inline SVG radar (pure stateless component, no recharts/d3 added) — eight axes match the new 8-dimension scoring set, with companion grid for raw scores. M6: each ScoreCard now shows a country chip (国家中文名 + 城市) resolved from snapshot.candidates; new VisaOverview section groups the curated visa routes by country represented in the report, including step-by-step weeks, post-study work years, and source citations. Also fixed a stale title on /[locale]/r/[code]/page.tsx that still read "你的澳洲院校推荐". M7: new ApplicationTimeline section turns visa processing weeks into a relative bar (max-normalized) so the student can see "AU 12 weeks vs CA 14 weeks" at a glance. M8: IM-style report chat. New core/ai/prompts/report-chat.ts (Zod ReportChatReplySchema = locale/reply/citations/followups, hard rule: no facts outside injected context, every claim cites a source_id) and core/ai/adapters/report-chat.ts (parse -> validate -> drop unknown source_ids). Web layer: apps/web/src/lib/ai/google-report-chat.ts binds the schema to Gemini via Vercel AI SDK generateObject; new POST /r/[code]/chat route flattens the snapshot into the prompt context and returns the post-filtered reply; new client component features/report/report-chat.tsx renders the chat surface with suggested questions, citations under each AI bubble, and clickable followups. Adapter test covers success / unknown-citation drop / validation failure / generation throw. Also re-numbered docs/prohibition.md so the new D-20 "禁止新增非必要实体" no longer collides with E-section numbers. 79 tests green. Note: apps/web/.next/types/validator.ts has stale references to the pre-i18n route paths (.next cache from before next-intl restructure); next dev/build will regenerate it. | feat/intake-multimodal |

| 2026-05-25 | feat(web,legal) | sprint 2 phase 3 M9 (A+D): contact loop + compliance. A: new /[locale]/contact page (server) with claymorphism ContactForm (channel chip + textarea, useActionState + useFormStatus), Zod-validated server action submitIntakeInquiryAction persisting to file-backed contact-store under apps/web/.data/contacts/{code}.jsonl (append-only). New client features/report/contact-card.tsx replaces the inline ContactCard in the report page; fires posthog.capture cta_clicked / code_revealed / report_generated (sessionStorage-deduped per code). D: 30-day share-link expiry on /[locale]/r/[code] with inline ExpiredReport fallback (bilingual). New /[locale]/legal/[doc] route reads legal/{PRIVACY,DISCLAIMER,TERMS}.md from repo root via a custom inline markdown renderer (no new dep, satisfies D-20). Footer added to [locale]/layout.tsx with the three legal links. i18n strings extended in messages/{zh,en}.json (common.footer + report.contactCard/expiryNote + contactPage + legal). 79/79 core tests still green; no new packages. | feat/intake-multimodal |
| 2026-05-25 | feat(web,admin) | sprint 2 phase 3 M10 (B+E): internal admin lookup + funnel events. B: new locale-free /admin route at apps/web/src/app/admin/page.tsx (server). Gated by ADMIN_TOKEN env var via length-aware constant-time-ish comparison; when env is unset, the route returns 404 unconditionally. Takes ?token=&code= query, validates Crockford report code regex, renders snapshot summary (code/created/target/budget/bucket totals) and a list of follow-up inquiries from .data/contacts/{code}.jsonl. New listContactInquiries(code) reader added to contact-store (ENOENT -> empty, malformed lines skipped). E: new shared apps/web/src/lib/analytics/track.ts exposing trackEvent() and trackEventOnce(); refactored contact-card and contact-form to use the helper (removes duplicated try/catch boilerplate, D-20). Wired intake_step_start / intake_step_complete / intake_step_skip on the form wizard (intake-form.tsx), and intake_submitted on form/chat/upload submit paths (with channel prop). .env.example documents ADMIN_TOKEN. 79/79 core tests still green; no new packages. | feat/intake-multimodal |

## 2026-05-25 �� Admin route fixes (commit 19ac2d9)

- proxy.ts: exclude /admin from next-intl matcher so the locale-free route is reachable without /zh prefix.
- app/admin/layout.tsx: added html/body wrapper (admin is outside [locale]/layout.tsx).
- app/admin/page.tsx: render target_level as em-dash when undefined.
- Discovered during M10 visual verification (manual browser test of /admin?token=&code=).

| 2026-05-25 | feat(intake-chat) | R1+R2: replace step-form chat with LLM-driven IM intake that runs the report directly. New apps/web/src/features/intake-chat/{chat-prompt.ts, chat-actions.ts} drive a 1-question-per-turn conversation; LLM (Gemini via @ai-sdk/google + generateObject) returns a strict ChatTurnSchema (reply/patch/quick_replies/done) that reuses ClarifyPatchSchema. When the LLM is unavailable a deterministic fallback emits the next missing-field prompt and parses common Chinese quick-reply phrases (target_level, city_size, teaching_style, preferred_tags, gpa, ielts, budget in CNY/AUD, target_field) so the conversation still advances. New apps/web/src/lib/run-report.ts extracts the candidates -> recommend -> narratives -> saveReport pipeline previously inlined in submitIntakeAction so it can be reused by finalizeChatIntakeAction (justified shared entity, used by 2 callers per prohibition #20). finalizeChatIntakeAction promotes the accumulated ClarifyPatch to a full StudentProfile (target_level defaults to master) and redirects to /r/{code}. R2 rewrites chat-intake.tsx into a true IM UI: bubble list with user gradient bubbles + assistant surface-alt bubbles, animated typing indicator (chatDot keyframes in globals.css), quick-reply chips above the input, top progress bar, and a header escape Direct-to-recommendation CTA enabled once target_level + 3 supporting signals exist. Also fixes the budget step=1000 issue in review-form.tsx (step=1 + inputMode=numeric). Verified end-to-end: chat -> ���ݿ�ѧ + 20������� + �þ�ҵ -> Direct CTA -> /r/UFCKSG renders full report. | (uncommitted) |
| 2026-05-25 | fix(ai,intake-chat) | Gemini was returning HTTP 404 from .../v1beta/models/:generateContent — model id was empty string. Root cause: `process.env.GOOGLE_TEXT_MODEL_ID ?? "gemini-2.5-flash"` does not catch the empty-string case produced when .env.local has `GOOGLE_TEXT_MODEL_ID=` with no value. Switched all five call sites (google-narrative.ts, google-extraction.ts, google-report-chat.ts, intake-chat/chat-actions.ts, intake-clarify/clarify-actions.ts) from `??` to `||` so empty string also falls back to the literal default. Also migrated from the deprecated `generateObject` to `generateText` + `Output.object` per AI SDK v6 guidance. Expanded the chat-intake fallback heuristics: budget regex now accepts pure 4-7 digit numbers (10k-99k -> AUD, >=100k -> CNY/4.7), target_field path emits a chip menu in the deterministic branch, mood detection (uncertain / frustrated / neutral) drives empathetic copy + chips, and FIELD_QUESTION_FINGERPRINT + countPriorAsks let the second ask for the same field use a rephrased prompt to avoid robotic repeats. Softened the degraded header text from "AI 暂时离线，已切到兜底问句" to "正在用基础模式陪你聊". | (uncommitted) |
| 2026-05-25 | fix(intake-chat) | Second-round UX pass after live user testing surfaced five friction points: (1) bare-number IELTS replies like "6.5" / "7.0" never extracted (heuristic required a 雅思/ielts keyword); (2) target_field "人类学" / "教育" etc could not be written because the enum rejects unknown values, so the bot kept asking; (3) explicit declines ("不知道", "不记得", "还没考", "跳过", "随便") were ignored so the same field was re-asked indefinitely; (4) `"IT"` in the heuristic field-map was not a valid enum value (real enum is `"Information Technology"`) — silently dropped; (5) when a field was asked 3+ times the LLM still repeated the same rephrase. Fixes: added optional `skipped_fields: FormFieldKey[]` to ClarifyPatchSchema (FORM_FIELD_KEYS const exported alongside) and threaded it through computeMissing / countSignals / canFinalize so a skipped field counts as a satisfied signal and is never re-asked. extractPatchHeuristic now takes `focus` (the field most recently asked) and (a) accepts bare numeric replies for IELTS in [0,9] when focus=ielts_overall, (b) detects skip-intent regex and either sets gpa/ielts_overall to 0 sentinel or pushes the field into skipped_fields, (c) replaces broken `"IT"` mapping with `"Information Technology"`. chatIntakeTurnAction computes the focus from a pre-merge `computeMissing` call and, when repeats>=2 for the head missing field, forcibly marks the field skipped and short-circuits the LLM call with the new acknowledgeSkipAndAsk helper ("好，这块先跳过。下一题…"). When the next field is also stuck the helper offers the "现在就看推荐" escape hatch instead of looping further. mergePatch now unions skipped_fields and preferred_tags arrays so neither side loses entries when the other returns a fresh patch. chat-prompt.ts: target_field now has chips (`["计算机","数据科学","金融","商科","工程","设计"]`), FIELD_LABELS_ZH corrected from `"IT"` to `"Information Technology"`, added rule 11 (no inline field-option lists in the reply text), rule 12 (append declined fields to skipped_fields and move on), rule 13 (no repeated apologies for the same limitation). | (uncommitted) |

| 2026-05-25 | feat(ai) | Switch primary LLM provider from Gemini to Groq after live testing exposed that the Gemini free tier (20 req/day for gemini-2.5-flash) was getting exhausted within minutes, causing the chat to spend the entire session in deterministic fallback. New dep `@ai-sdk/groq@^2` (justified: alternative provider with substantially higher free-tier limits and better stability; Gemini retained as automatic fallback when GROQ_API_KEY is absent). google-narrative.ts now exposes `getTextModel()` which prefers Groq (model id from `GROQ_MODEL_ID`, default `llama-3.3-70b-versatile`) and falls back to Gemini, and `isLLMConfigured()` returns true if either provider key is set (`isGoogleConfigured` retained as a backward-compatible alias to avoid churning 7 import sites in a hot-fix). All four LLM call sites updated to use `getTextModel()`: lib/ai/google-extraction.ts, lib/ai/google-report-chat.ts, features/intake-chat/chat-actions.ts, features/intake-clarify/clarify-actions.ts. .env.local template now leads with GROQ_API_KEY/GROQ_MODEL_ID. docs/techstack.md section 3 updated. | (uncommitted) |

| 2026-05-26 | feat(intake) | step 3 personalized assessment added between chat and report per spec FR-2. New apps/web/src/features/assessment/ module: items.ts defines 10 TIPI Big Five items (Likert 1-5, reverse-keyed pairs averaged then scaled to 0-7 to match BigFiveSchema) plus 5 mixed preference items (teaching_style enum, class_size_small Likert, city_size enum, migration_intent Likert -> career.return_home derived as 6-x proxy, fast_pace Likert); assessment-actions.ts exposes finalizeAssessmentAction(chatPatch, answers) that merges scoreAssessment() output with the chat ClarifyPatch into a full StudentProfile (assessment values win on overlap), runs runReportFromProfile, redirects to /r/{code}; assessment-flow.tsx is the client UI (sticky progress, claymorphism Likert cards with anchor labels, enum chips, submit gated until all 15 answered). New route /[locale]/intake/assessment. chat-intake.tsx onFinalize now stashes the patch in sessionStorage (key isp_chat_patch_v1) and router.push(/intake/assessment); finalizeChatIntakeAction kept as fallback when sessionStorage unavailable. Closes the personality/preference data gap that left passive users with sparse profiles. | feat/intake-multimodal |

| 2026-05-26 | refactor(intake) | step 3 assessment promoted to step 1 and rebuilt as one-question-at-a-time Duolingo-style flow. Hub chat door now points to /intake/assessment; assessment stores answers in sessionStorage (isp_assessment_v1) then router.push(/intake/chat); chat-intake onFinalize reads the stashed assessment and passes it to finalizeChatIntakeAction(accumulated, assessment) which scoreAssessment-s it inside patchToProfile to seed big_five/learning/lifestyle/career. UX: one TIPI or preference per card, full-width Likert/enum buttons, auto-advance after a tap, back arrow + slim top progress bar, final card waits on an explicit done button. assessment-actions.ts emptied as a placeholder. | feat/intake-multimodal |

| 2026-05-26 | feat(profile) | decouple academic signal from a hard-coded 4.0 GPA. New AcademicCredentialSchema {kind, raw, note?} with 14 kinds covering gpa_4 / gpa_5 / wam_100 / percentage_100 / uk_class / pass_fail / gaokao / ap / alevel / ib / sat / act / certificate / other; added to AcademicBackgroundSchema as credentials[] (default []). New pure normalizer packages/core/src/rules/normalize/academic-grade.ts: normalizeCredential parses raw text per kind (gaokao /750 default with explicit max override, AP avg /5, A-level letter-tier with A* token handling, WAM with banded curve, UK class word matching incl. Chinese synonyms һ��/����һ/���ȶ�), summarizeCredentials picks highest-confidence usable credential, getEffectiveGpa4 prefers explicit legacy gpa then falls back to summary when confidence >= 0.5. Free-form certificate/other return zero confidence so they never push or punish the score. academic-fit.ts and thresholds.ts now consume getEffectiveGpa4 instead of profile.academic.gpa directly. ClarifyPatchSchema extended with optional credentials field; computeMissing and canFinalize treat credentials.length > 0 as satisfying the gpa slot so a student who shared their gaokao or AP scores is never re-asked for a 4.0 GPA. chat-prompt.ts rule 11a instructs the LLM to write raw values into credentials[] and never to convert (examples for each kind), fallback gpa quick replies updated to [�߿���, ���� GPA, WAM, A-level, AP, ֤��]. New academic-grade.test.ts (13 cases). All 92 core tests pass. Admin page typecheck errors are pre-existing and out of scope. | feat/intake-multimodal |

| 2026-05-26 | feat(intake) | wire assessment results into the chat LLM; add psychology-authority cues; diversify chat interaction modes; fix recurring LLM schema-mismatch failure. (1) chat-intake.tsx now reads sessionStorage.isp_assessment_v1 on mount and forwards it on every chatIntakeTurnAction call, not only at finalize. chat-actions.ts scoreAssessment-s it and threads the Big Five + preference summary into the system prompt via a new optional ChatIntakePromptInput.assessment field. The prompt now instructs the LLM to open with a one-sentence callback to the most salient trait ("看你尽责性挺高的, 做事挺有计划的人") and to skip re-asking teaching_style / city_size when the assessment already supplied them. Student stops feeling the chat ignored the 15-question test. (2) assessment-flow.tsx top header now carries the literal "基于 Big Five 人格量表 (TIPI · Gosling, Rentfrow & Swann, 2003) + 学习/生活偏好量表" line; per-TIPI-card section tag now reads "Big Five · 外向性" / "尽责性" / ... so the user can see which trait each question maps to, reinforcing scientific framing. Footer copy updated to promise the result will be handed off to the chat layer. (3) ChatTurnSchema gains optional input_mode ∈ "single" | "multi" | "number"; chat-intake.tsx renders multi-select toggle chips with a "就这几个" confirm button when multi, and a range slider with min/max/step/unit (encoded as one quick_replies string "min,max,step,unit") when number. Prompt now instructs the LLM to use multi for preferred_tags and number for annual_budget_aud / ielts_overall. (4) ClarifyPatchSchema and ChatTurnSchema dropped .strict() so unknown keys the LLM emits are stripped silently instead of killing the whole turn ("No object generated: response did not match schema." root cause). Rule 11a rewritten with explicit JSON-shape examples (`{"credentials": [{"kind": "gaokao", "raw": "680"}]}` etc.) and an inline-listed allowed-kind enum, eliminating the ambiguous "kind: example" key:value listing that confused gpt-oss-120b. No new core tests needed (all 92 still pass); web typecheck clean for changed files (admin/page.tsx errors remain pre-existing). | feat/intake-multimodal |

| 2026-05-26 | feat(assessment,chat) | RIASEC + result review + full data into chat. (1) packages/core: new CareerInterestsSchema (6 Holland dims, 0..1, public-domain framework �� Holland 1959; in-house items, no SDS/SII reuse) added to CareerPreferenceSchema as optional. (2) apps/web assessment: 6 in-house RIASEC Likert items added to apps/web/src/features/assessment/items.ts (RIASEC_ITEMS, scored into career.interests by (L-1)/4); assessment-flow.tsx now 21 questions (TIPI 10 + RIASEC 6 + prefs 5), authority subheader cites both TIPI & Holland, finish CTA copy changed to `��ɣ��鿴��������`. (3) New review page apps/web/src/app/[locale]/intake/assessment/result/page.tsx + apps/web/src/features/assessment/assessment-result-review.tsx �� reads sessionStorage isp_assessment_v1, calls scoreAssessment(), shows 5 Big Five bars (�����ȶ��� inverts neuroticism), top-3 RIASEC bars with expandable full-6 details, and quantified learning/lifestyle/career rows; primary CTA routes to /intake/chat. (4) chat-prompt.ts AssessmentSummaryForPrompt extended with interests + learning.class_size_small/fast_pace + career.migration_intent/return_home; renderAssessmentBlock now also surfaces RIASEC top-3 (with Chinese names) and full quantified prefs to the LLM, and adds rule for the LLM to prefer high-RIASEC fields in target_field quick_replies. chat-actions.ts forwards the wider scored payload. 92/92 core tests green; no new dependencies. | feat/intake-multimodal |

| 2026-05-26 | fix(intake-chat) | P0+P1: slot locking + intake FSM + cross-reload patch persistence. ROOT CAUSE: chat kept re-asking answered fields (`anthropology ���Ҳ���˵����`) and occasionally returned empty bubbles. FIX: new pure helper apps/web/src/features/intake-chat/intake-state.ts exports isFieldFilled/lockedKeys/unlockedKeys/nextPhase/sanitizePatch/mergePatchDeep. (P0-A) chat-intake.tsx now persists accumulated to sessionStorage under isp_intake_accumulated_v1 on every patch and restores on mount, using mergePatchDeep client-side so preferred_tags / skipped_fields / credentials union instead of overwrite; key is cleared together with isp_assessment_v1 on successful finalize. (P0-B) chat-actions.ts computeMissing rewritten in terms of lockedKeys (already-filled or skipped fields drop out automatically); sanitizePatch drops any LLM attempt to overwrite a locked field or to write unknown top-level keys before mergePatchDeep folds the increment in; chat-prompt.ts gets a new LOCKED FIELDS section that explicitly forbids re-asking them. (P0-C) prompt also gets an explicit FSM phase block �� OPENING / GATHERING_CORE / GATHERING_SOFT / READY_TO_RECOMMEND, picked by nextPhase(accumulated, hasUserMessage) �� with phase-specific guidance (e.g. OPENING with assessment must reference a trait; READY_TO_RECOMMEND forbids new questions). (P1-A) sanitizePatch + Zod-without-strict together guarantee schema parse failures degrade to a no-op patch instead of empty bubbles, and credentials remain writable even after gpa is locked. (P1-B) FSM lives as pure functions, no new dep, no XState. 92/92 core tests still green. | feat/intake-multimodal |

2026-05-26 | feat(data) | seed 18 bachelor programs (AU 6 + UK 6 + CA 6) across existing universities to unblock bachelor recommendation path; sticker prices placeholder, sources flagged 'verify before launch'; no schema change (StudyLevelSchema already permits bachelor); 92/92 core tests green.

2026-05-26 | refactor(intake-chat) | P2 multi-agent split of the IM intake LLM (Option A, two agents). MOTIVATION: a single 17-rule system prompt was both extracting structured patches AND running the conversational FSM, leading to drift (occasional patch field invented, locked-field overwrite attempts, quick_replies missing on enum fields). FIX: chat-actions.ts now orchestrates two focused LLM calls per turn instead of one. (1) Extraction Agent (EA): new apps/web/src/features/intake-chat/chat-prompt-extraction.ts exposes ExtractionOutputSchema {patch?: ClarifyPatch} + buildExtractionSystemPrompt(accumulated, lockedKeys, lastAssistantQuestion, lastUserMessage); EA only runs when the user has actually spoken; its job is strictly to map free-text -> ClarifyPatch obeying never-invent / locked-fields / enum-exact / CNY->AUD / credentials-as-array / vague-term-mapping / skip-detection rules. EA failure (rate limit, parse) is isolated: console.warn + cleanPatch = {} and we still run CA so the user is never stuck. (2) Conversation Agent (CA): new apps/web/src/features/intake-chat/chat-prompt-conversation.ts exposes ConversationOutputSchema {reply, quick_replies?, input_mode?, done} + buildConversationSystemPrompt(accumulated, missingKeys, lockedKeys, phase, assessment?); CA is told explicitly that extraction has ALREADY happened and that it must NOT emit a patch, freeing it to focus purely on tone / FSM phase / quick_replies / done-gate. CA failure falls back to fallbackQuestion(missingAfter) while preserving EA's cleanPatch. (3) chat-actions.ts orchestrator: hasUserMessage -> EA -> sanitizePatch(input.accumulated, output.patch) ?? {} -> mergePatchDeep -> recompute lockedAfter/missingAfter -> nextPhase(merged, {hasUserMessage}) -> CA; finalDone = CA.done && (canFinalize(merged) || userRequestedStop). (4) chat-prompt.ts trimmed to shared constants/helpers only (FIELD_LABELS_ZH, CHAT_PRIORITY, MIN_SUPPORTING_SIGNALS, AssessmentSummaryForPrompt, renderAssessmentBlock, renderPhaseBlock); buildChatIntakeSystemPrompt + ChatIntakePromptInput removed. ADR-0002 docs the decision and the rejected alternatives (3-agent router, schema hardening only, async extraction). TRADE-OFFS: ~2x LLM latency per user turn (mitigated by EA prompt being short + considered for a smaller model later), two prompts to keep in sync if ClarifyPatch shape evolves. Core tests still 92/92 green; @isp0526/web typecheck clean on all P2 files (4 pre-existing admin/page.tsx errors unrelated). | feat/intake-multimodal |
