# 变更记录（Change Record）

> 每个非空 PR 必须在此追加条目。格式：`YYYY-MM-DD | <type>(<scope>) | <subject> | <PR #>`
> 此文件为 Release Notes 的事实源。版本 tag 前抓取相应区间内容。

---

## v0.0.0 — Baseline（仓库初始化）

| 日期 | 类型 | 摘要 | PR |
|---|---|---|---|
| 2026-05-23 | docs(spec) | initial product spec v1.0 | — (pre-repo) |
| 2026-05-23 | chore(repo) | initialize repo skeleton with docs, legal, copilot instructions | — (initial commit) |
| 2026-05-23 | chore(mcp) | add Figma MCP server (Framelink) for design token extraction | — (direct main) |
| 2026-05-23 | docs(spec) | section 7 rewritten as Warm Claymorphism design system with full token table (colors / radii / shadows / typography) extracted from Meng To reference + warm palette adaptation | — (direct main) |
| 2026-05-23 | feat(scaffold) | sprint 0: scaffold `apps/web` (Next.js 16 + TS strict + Tailwind v4) and `packages/core` (rules/ai/data/schemas); write warm claymorphism tokens into `globals.css`; add token-preview landing page; add i18n message files (zh/en); add Supabase env helper + `.env.example`; add GitHub Actions CI (lint+typecheck+test+build) | feat/scaffold-web-and-core |
| 2026-05-23 | refactor(spec) | business model adjusted: users no longer routed to commercial agents directly. Reports now end in a low-key "want to talk to someone?" card that opens a Contact page (WhatsApp / WeChat to our team + report ID). Agents remain a backend referral channel, invisible to end users. Renamed `TransferCode` -> `ReportCode`, updated FR-5.3 / FR-6 / FR-9.3 / NFR-5.5 / data model / KPIs / risks accordingly. i18n message files updated. | feat/scaffold-web-and-core |
| 2026-05-23 | docs | sync spec 7.2.1 color/shadow tables with desaturated palette (round 2); update techstack.md to Next 16 / TS 5.9 / Node 22 / pnpm 11.2.2 and document `allowBuilds` constraint | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | sprint 1 step 1: introduce Zod schemas (`ids`, `source`, `institution`, `student-profile`, `scoring`) as the single source of truth for the rule engine and the LLM contract; add 5 Go8 placeholder rows in `packages/core/data/{universities,programs}.au.json` with `source_id` citations; wire static data loader that validates JSON at module init; implement `thresholds.applyHardThresholds`, `bands.classifyBand`, and the first scoring dimension `academic-fit` with 7 unit tests; wire vitest, allow esbuild postinstall in `pnpm-workspace.yaml` | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | sprint 1 step 2: complete 7-dimension scoring engine — add `personality`, `lifestyle`, `career`, `budget`, `tag_boost`, `reputation` pure scoring functions with shared `__fixtures__` and 26 colocated tests; add weight personalization (`modulate.ts`, doubles budget weight on high salary sensitivity); add aggregator `score.scoreCandidate` that combines the 7 dimensions into a validated `Score` (per-dimension breakdown, 0..100 final score, band, cited reasons); add `recommend.recommend` pipeline (hard thresholds -> score -> sort -> bucket caps 5/10/5); 41 tests green | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | sprint 1 step 3: AI adapter scaffold — add `ai/result.ts` Result type with `AIError`; `ai/config.ts` env-driven `getModel(role)`; `ai/prompts/recommendation-narrative.ts` (Zod schema + system prompt + structured user-prompt builder, bilingual zh/en); `ai/post-filter.ts` rejecting narratives whose citations are not in the known source-id set; `ai/adapters/narrative.ts` injectable `GenerateObjectFn` so the Vercel AI SDK can be wired without coupling business code. 53 tests green; no new runtime dependencies | feat/scaffold-web-and-core |
| 2026-05-23 | chore(core) | drop trailing `.js` extension from all relative import specifiers under `packages/core/src/**` so the package can be consumed as raw TypeScript via Next.js `transpilePackages` without precompiling. Pure mechanical rewrite (tsc bundler resolution accepts both forms); 58 tests still green | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | sprint 1 step 4: end-to-end free-tier flow. Add `packages/core/ai/templates/narrative-template.ts` producing the same `RecommendationNarrative` schema as the LLM adapter (deterministic, zero API cost), `packages/core/reports/code.ts` generating 6-char Crockford-style `ReportCode`. Add `apps/web` intake form (server action + `useFormStatus`) → `recommend()` pipeline → in-memory report store → `/r/[code]` report page with `ScoreCard` + `ContactCard` + per-dimension breakdown. Landing CTA now routes to `/intake`. `next.config.ts` gets `transpilePackages: ["@isp0526/core"]`; `zod` added to web dependencies | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | expand AU program seed data 5 → 21 programs across 5 universities (ANU/UniMelb/UNSW/USyd/UQ) covering IT, Computing, Data Science, Business, Finance, MBA, Civil/Electrical/Mechanical Engineering, Design, TESOL. All entries still carry `placeholder sticker price; verify before launch`; URLs point to canonical program pages | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | file-backed report store under `apps/web/.data/reports/{code}.json` (gitignored) with per-process in-memory cache; replaces the volatile globalThis Map so restarting `next dev` no longer 404s historical report codes. `saveReport` / `loadReport` are now async; callers updated. | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | intake form `target_field` becomes a curated dropdown (`apps/web/src/features/intake/field-options.ts`) whose 11 values are 1:1 with `programs.au.json` `field` strings; removes the free-text trap that was zeroing out the field-match dimension | feat/scaffold-web-and-core |
| 2026-05-23 | feat(core) | add batch narrative pipeline: `prompts/recommendation-narrative-batch.ts` (Zod schema wrapping `narratives: Array<RecommendationNarrative>`) + `adapters/narrative-batch.ts` reusing per-item `filterNarrative` and returning a `{ narratives, rejected }` map so callers can fall back per slot. 63 tests green (added 5) | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | wire Gemini 2.0 Flash (free tier) via Vercel AI SDK (`ai` ^6, `@ai-sdk/google` ^3). `apps/web/src/lib/ai/google-narrative.ts` injects `generateObject` into the core batch adapter. `intake-actions.ts` calls LLM first and falls back to the deterministic template for any rejected / missing item, or for the whole batch when the API key is absent or the call throws. `.env.example` updated; `docs/techstack.md` AI section rewritten | feat/scaffold-web-and-core |
| 2026-05-23 | feat(web) | sprint 2 step E1a: restructure `/intake` into a 4-door hub (form / upload / voice / chat) with claymorphism cards; move the structured form to `/intake/form` so the entry-point can host multiple intake channels. Voice / chat tiles render as "coming soon" until E2/E3 land | feat/intake-multimodal |
| 2026-05-23 | feat(web) | sprint 2 step E1b: `/intake/upload` page + `UploadDropzone` client component (drag/drop, multi-file, per-file progress + status badge). PDF text extraction runs client-side via `pdfjs-dist` in main-thread mode (`disableWorker:true`) to sidestep Turbopack worker plumbing. Extracted text currently logs to dev console; AI hook-up follows in E1d/E1e | feat/intake-multimodal |
| 2026-05-23 | feat(core) | sprint 2 step E1c: intake extraction layer in the AI package - `prompts/intake-extraction.ts` defines `ExtractedProfileSchema` (every field wrapped with `{value, confidence, source_excerpt}`), bilingual system prompt forbidding fabrication; `adapters/intake-extraction.ts` parses Zod, drops sub-floor confidence (default 0.4), surfaces `Result<ExtractedProfile, AIError>`. 7 new unit tests cover empty input, parse path, confidence floor + override, schema invalid, generation error, default exported. 70/70 tests green | feat/intake-multimodal |
| 2026-05-23 | feat(web) | sprint 2 steps E1d + E1e: end-to-end upload -> review -> submit flow. New file-backed intake session store under `apps/web/.data/intake-sessions/{uuid}.json` (gitignored, in-process cache). New server action `startIntakeFromTextAction` calls Gemini via a new `buildGoogleExtractionGenerator` (binds `ExtractedProfileSchema` to Vercel AI SDK `generateObject`), persists the session, returns the session id. Upload dropzone now POSTs combined extracted text to that action and routes to `/intake/review/[id]`. New `/intake/review/[id]/page.tsx` server component loads the session and renders the new client `ReviewForm`: split layout (left = source label + scrollable original text + AI notes; right = the canonical intake fields, each pre-filled from the extracted profile and tagged with a confidence badge - green "AI 高置信" for >=0.7, yellow "请核对" for <0.7, gray "已修改" once the user edits, with the source excerpt rendered as a quoted caption below the input). Free-text discipline matched onto the curated FIELD_OPTIONS via substring; unmatched values fall back to manual selection. The review form submits to the existing `submitIntakeAction` so the rest of the report pipeline is unchanged | feat/intake-multimodal |
| 2026-05-24 | feat(web) | sprint 2 step E1f (partial): DOCX upload support via mammoth ^1 browser bundle. New `apps/web/src/lib/docx/extract-docx-text.ts` lazy-imports `mammoth/mammoth.browser.js`, runs `extractRawText({arrayBuffer})`, returns plain text. Dropzone now accepts `.pdf` and `.docx` (mime + extension fallback for browsers that drop the docx mime), routes each file to the right extractor, and the rest of the pipeline (combine -> server action -> review) is unchanged. Added a local `.d.ts` shim because mammoth ships no types for the browser entry. Image-based intake (Gemini Vision) intentionally deferred | feat/intake-multimodal |
| 2026-05-24 | feat(web) | sprint 2 steps E2a + E2b + E2c: voice intake channel. New `/intake/voice` page; client `VoiceOrb` (claymorphism orb, idle / requesting / listening / captured / uploading / error states, 60 s auto-stop with on-orb countdown, replay + redo + submit controls). Recording uses MediaRecorder with a candidate-mime list (webm/opus -> webm -> mp4 -> mpeg) so it works in Chromium, Firefox, and Safari. New server action `startIntakeFromAudioAction(formData)` reads the audio Blob, calls Gemini 2.0 Flash via `generateText` with a multimodal `file` part (transcribe-only prompt, original-language preserved), then reuses the existing `extractProfileFromText` + `buildGoogleExtractionGenerator` to fill `ExtractedProfile`, persists an intake session with `source.kind='voice'`, and routes the user to the existing `/intake/review/[id]`. Hub door #3 unlocked. 15 MB hard cap server-side for safety | feat/intake-multimodal |

---

<!-- 未来条目追加到上方 v0.0.x 区段，按时间倒序 -->
| 2026-05-24 | fix(intake-extraction) | sprint 2 voice-channel calibration: relax the extraction system prompt so currency-less budget phrases (e.g. "20��") default to AUD at confidence 0.5..0.65, and broad Chinese discipline terms (e.g. "����", "�̿�") normalize to the closest English discipline name at confidence 0.55..0.7; also widen the confidence ladder to 0.4..0.6 for "supported but requires assumption". Add dev-only `console.info` of raw LLM output before the confidence floor for debug visibility. 70/70 core tests still green | feat/intake-multimodal |
| 2026-05-24 | feat(intake-clarify) | sprint 2 step C1: add post-intake clarification chat as the mandatory last leg of every intake channel. New feature module apps/web/src/features/intake-clarify/ with: clarify-schema.ts (Zod ClarifyPatchSchema + ClarifyTurnSchema constrained to FIELD_OPTIONS / teaching style / city size / preferred tags enum values), clarify-prompt.ts (Chinese-only warm advisor system prompt, one-question-per-turn, normalizes vague answers to enums, defaults currency-less budget to AUD), clarify-actions.ts (server action calling generateObject + gemini-2.0-flash with the turn schema, returns reply + optional patch + done flag), clarify-chat.tsx (controlled chat surface, scroll-to-latest, Enter-to-send). Refactor review-form.tsx to controlled state so chat patches can update inputs live, replace edited Set with 3-state origin map (ai / confirmed / edited), add green "AI ׷����ȷ��" badge for chat-patched fields. Review page now passes sessionId. Build green. | feat/intake-multimodal |
